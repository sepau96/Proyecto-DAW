$conexion = mysqli_connect("localhost", "root", "", "changingroom");

if ($conexion){
	$user = $_POST["user"];

	if(isset($_REQUEST["submit"])){
		if ($resultado = mysqli_query($conexion, "SELECT user, email FROM users WHERE user = '$user'")) {
                        $numcolumnas = mysqli_num_rows($resultado);       
                    }

                    if ($numcolumnas == 0){}
